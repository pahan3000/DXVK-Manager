<Window x:Class="DxvkInjector.DxvkScanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DxvkInjector"
        Title="DXVK Injector — Library Scan"
        Height="560" Width="720"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:BoolToVisibilityConverter x:Key="ScanBoolToVisibilityConverter"/>
            <local:InverseBoolToVisibilityConverter x:Key="ScanBoolToInverseVisibilityConverter"/>
        </ResourceDictionary>

    </Window.Resources>

    <!-- Explicit Background/Foreground here (not just the implicit TargetType="Window"
         style in DarkTheme.xaml) so Playnite's own global window styling can't leave this
         white with unreadable text — a local value always wins over a style setter. -->
    <Border Background="{StaticResource DxvkBg}" TextElement.Foreground="{StaticResource DxvkText}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Library scan" Style="{StaticResource DxvkSectionHeader}"
                   Margin="0,0,0,6" FontSize="16"/>

        <TextBlock Grid.Row="1" Text="{Binding SummaryText}" Style="{StaticResource DxvkStatusText}"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Rescan" Command="{Binding ScanCommand}" Width="90" Margin="0,0,8,0"/>
            <Button Content="Select all eligible" Command="{Binding SelectAllEligibleCommand}" Width="130" Margin="0,0,8,0"/>
            <Button Content="Select none" Command="{Binding SelectNoneCommand}" Width="100"/>
        </StackPanel>

        <Border Grid.Row="3" Style="{StaticResource DxvkPanel}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header row -->
                <Grid Grid.Row="0" Margin="10,8,10,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="1" Text="Game" Style="{StaticResource DxvkMutedText}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="D3D API" Style="{StaticResource DxvkMutedText}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Arch" Style="{StaticResource DxvkMutedText}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Status" Style="{StaticResource DxvkMutedText}" FontWeight="SemiBold"/>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Results}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource DxvkBorder}" BorderThickness="0,0,0,1" Padding="10,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="30"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="2*"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0" VerticalAlignment="Center"
                                                  IsChecked="{Binding Selected}"
                                                  IsEnabled="{Binding Eligible}"
                                                  Visibility="{Binding AlreadyInjected, Converter={StaticResource ScanBoolToInverseVisibilityConverter}}"/>

                                        <TextBlock Grid.Column="1" Text="{Binding GameName}" VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2" Text="{Binding ApiLabel}" VerticalAlignment="Center"
                                                   Style="{StaticResource DxvkMutedText}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding ArchLabel}" VerticalAlignment="Center"
                                                   Style="{StaticResource DxvkMutedText}"/>
                                        <TextBlock Grid.Column="4" Text="{Binding StatusText}" VerticalAlignment="Center"
                                                   Style="{StaticResource DxvkMutedText}" TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <ProgressBar Grid.Row="4" IsIndeterminate="True" Height="4" Margin="0,10,0,0"
                     Visibility="{Binding Busy, Converter={StaticResource ScanBoolToVisibilityConverter}}"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Inject selected" Command="{Binding InjectSelectedCommand}" Width="140"
                    Style="{StaticResource DxvkPrimaryButton}"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
